database:
  schema: ${DB_SCHEMA:as_app_messaging}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:147.93.4.13}:${DB_PORT:5432}/${DB_NAME:00_drey}?currentSchema=${database.schema}
    username: ${DB_USER:db.drey}
    password: ${DB_PASSWORD:nEw<VE4.62Tjs0*}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: ${database.schema}
        format_sql: true
    show-sql: true

